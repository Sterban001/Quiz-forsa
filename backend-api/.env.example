PORT=4000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# CORS Configuration
# IMPORTANT: In production, set this to your actual frontend domains
# Example for production: https://admin.yourdomain.com,https://app.yourdomain.com
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# JWT Secret (not currently used, but reserved for future use)
JWT_SECRET=your-jwt-secret

# Redis Configuration (optional - app works without it)
# For local development: redis://localhost:6379
# For Redis Cloud: redis://default:PASSWORD@ENDPOINT:PORT
# Redis is used for rate limiting storage - without it, in-memory storage is used
REDIS_URL=redis://localhost:6379

# Security Notes:
# 1. Never commit the actual .env file to version control
# 2. In production, set NODE_ENV=production to enable:
#    - Secure cookies (HTTPS only)
#    - HSTS headers
#    - Production error handling
# 3. Update CORS_ORIGIN to match your production domains
# 4. Use strong, unique secrets for SUPABASE_SERVICE_ROLE_KEY
